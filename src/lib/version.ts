/** Desktop app version. Bump by 0.1 on every commit, same convention as
 *  the SpendLens phone app. Shown in the sidebar footer so you can
 *  confirm at a glance which build is deployed. */
export const APP_VERSION = '0.2';
